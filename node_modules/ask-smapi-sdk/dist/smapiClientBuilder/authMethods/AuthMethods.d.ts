export interface RefreshTokenConfig {
    clientId: string;
    clientSecret: string;
    refreshToken: string;
}
export interface AccessTokenConfig {
    clientId: string;
    clientSecret: string;
    accessToken: string;
}
